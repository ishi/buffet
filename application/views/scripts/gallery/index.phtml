<table id="galleries">
	<tr>
		<?php foreach ($this->galleries as $gallery) : ?>
		<td>
			<div class="gallery">
				<div class="photos clearfix">
					<div class="preview">
						<img src="<?php echo $gallery->getMainPhoto()->getCroppedName() ?>" />
					</div>
					<div class="thumbnails clearfix">
						<?php foreach ($gallery->getActivePhotos() as $photo) : ?>
							<div class="thumbnail"><img src="<?php echo $photo->getThumbnailName() ?>" /></div>
						<?php endforeach ?>
					</div>
				</div>
				<div class="description">
					<h4><?php echo $gallery->getFolderDate() ?></h4>
					<p><?php echo $this->escape($gallery->getFolderName()) ?></p>
				</div>
			</div>
		</td>
		<?php endforeach ?>
	</tr>
</table>
